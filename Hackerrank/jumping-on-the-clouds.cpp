#include<bits/stdc++.h>
using namespace std;
int t, N;
vector<int> p;

int min(int a, int b) {
    return a < b ? a : b;
}

int dp(int idx) {

} 

int main() {
    cin >> N;
    while(N--) {
        cin >> t;
        p.push_back(t);
    }
    cout << dp() << endl;


    return 0;
}